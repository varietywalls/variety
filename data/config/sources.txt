# These are automatically appended to the user's sources.
# New default sources should be added here, not in the default config so that existing users will also see them
# (they already have a config file, so the default config is not used for them)

False|favorites|The Favorites folder
True|fetched|The Fetched folder
False|desktoppr|Random wallpapers from Desktoppr.co. May contain nudity and porn.
False|apod|NASA's Astronomy Picture of the Day
